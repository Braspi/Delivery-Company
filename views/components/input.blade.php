<label for="{{ $id }}"></label>
<input type="{{ $type }}"
       id="{{ $id }}"
       placeholder="{{ $placeholder }}"
       value="{{ $value }}"
       class="w-96 border border-gray-500 p-2 rounded-md focus:outline-none focus:border-blue-500 block mb-4 {{ $class }}"
/>